# Analisis Prisma RTS - dependensi aplikasi lokal (Streamlit).
# Untuk mesin analisis saja (batch/CI/skrip reproduksi) gunakan
# requirements-core.txt yang hanya berisi pandas + numpy.
# TIDAK memerlukan: GDAL, geopandas, shapely, fiona, scipy, PostGIS, Docker.
# GeoPackage ditulis langsung lewat modul sqlite3 bawaan Python (gpkg_lite.py).
# Regresi Theil-Sen diimplementasikan dengan numpy (robust.py).

streamlit>=1.30
pandas>=2.0
numpy>=1.24
